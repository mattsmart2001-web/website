[build]
  publish = "."
  command = "echo 'No build needed - static site'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  # Scheduled function to auto-update leaderboard stats
  [functions."update-leaderboard"]
    schedule = "0 * * * *"  # Every hour at minute 0
