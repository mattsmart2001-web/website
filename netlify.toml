[build]
  publish = "."
  command = "echo 'No build needed - static site'"

[build.environment]
  # gtec-config.js intentionally contains the public Supabase anon key and URL.
  # These are client-side credentials by design — not secrets.
  SECRETS_SCAN_OMIT_PATHS = "endurance/assets/gtec-config.js"

# Legacy Shopify URLs. The site previously ran a store under these paths
# and Google still has them in its index. Return HTTP 410 (Gone) so Google
# drops them rather than treating them as soft duplicates of the homepage,
# which is what the catch-all below would otherwise do.
[[redirects]]
  from = "/products/*"
  to = "/gone"
  status = 410
  force = true

[[redirects]]
  from = "/collections/*"
  to = "/gone"
  status = 410
  force = true

[[redirects]]
  from = "/pages/*"
  to = "/gone"
  status = 410
  force = true

[[redirects]]
  from = "/policies/*"
  to = "/gone"
  status = 410
  force = true

[[redirects]]
  from = "/supporters/*"
  to = "/gone"
  status = 410
  force = true

[[redirects]]
  from = "/en-eur"
  to = "/gone"
  status = 410
  force = true

[[redirects]]
  from = "/en-eur/*"
  to = "/gone"
  status = 410
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  # DISABLED: Scheduled function to auto-update leaderboard stats
  # Disabled for ethical review - awaiting permission from gtstats.live
  # [functions."update-leaderboard"]
  #   schedule = "0 * * * *"  # Every hour at minute 0
