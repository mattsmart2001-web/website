# PyInstaller version resource for GT7-Live-Connector.exe.
# Real version metadata makes the build look like the legitimate app it is,
# which reduces (though cannot fully eliminate) antivirus false positives.
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(2, 1, 0, 0),
    prodvers=(2, 1, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo([
      StringTable('040904B0', [
        StringStruct('CompanyName', 'SparksTheory'),
        StringStruct('FileDescription', 'GT7 Live Telemetry Connector'),
        StringStruct('FileVersion', '2.1.0.0'),
        StringStruct('InternalName', 'GT7-Live-Connector'),
        StringStruct('LegalCopyright', 'SparksTheory'),
        StringStruct('OriginalFilename', 'GT7-Live-Connector.exe'),
        StringStruct('ProductName', 'GT7 Live Connector'),
        StringStruct('ProductVersion', '2.1.0.0')
      ])
    ]),
    VarFileInfo([VarStruct('Translation', [1033, 1200])])
  ]
)
